#ifndef DELAY_H
#define DELAY_H

/**
 * @brief Generates a delay by performing a busy-wait loop.
 * @param[in] count The number of iterations for the busy-wait loop.
 */
void delay(int count);

#endif  // DELAY_H
